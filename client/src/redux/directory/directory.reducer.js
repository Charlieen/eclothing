import  SECTIONS_DATA from '../../data/sections.data';

const INITIAL_DATA= {
    sections:SECTIONS_DATA
}

const directoryReducer = (state = INITIAL_DATA) => {
        return state;
    }
export default directoryReducer;